---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getEntry, render } from 'astro:content';

const bio = await getEntry('pages', 'bio');
if (!bio) throw new Error('Missing bio content');
const { Content } = await render(bio);
---

<BaseLayout title="Biografía" description="Biografía de Araceli Bonfigli">
  <header class="page-hero">
    <div class="container">
      <p class="section-eyebrow">Trayectoria</p>
      <h1>{bio.data.title}</h1>
    </div>
  </header>

  <div class="container section bio">
    <div class="bio-photos reveal">
      {
        bio.data.images?.map((image) => (
          <img src={image.src} alt={image.alt ?? 'Araceli Bonfigli'} loading="lazy" />
        ))
      }
    </div>
    <div class="bio-copy reveal">
      <Content />
    </div>
  </div>
</BaseLayout>

<style>
  .bio {
    display: grid;
    grid-template-columns: minmax(14rem, 22rem) 1fr;
    gap: var(--space-lg);
    align-items: start;
  }

  .bio-photos {
    display: grid;
    gap: 0.85rem;
  }

  .bio-copy {
    font-size: 1.08rem;
    color: var(--color-ink-soft);
  }

  .bio-copy :global(p) {
    margin-bottom: 1.1rem;
  }

  .bio-copy :global(strong) {
    color: var(--color-ink);
    font-weight: 600;
  }

  @media (max-width: 800px) {
    .bio {
      grid-template-columns: 1fr;
    }

    .bio-photos {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
